1. useradd student -g wheel -p 123456! -s /bin/bash -u 1000
2. hostnamectl set-hostname student
3. yum update
4. yum install nano
   yum install mc
   yum install net-tools
5. yum groupinstall "Development Tools"
   yum install ncurses ncurses-devel
   wget http://hisham.hm/htop/releases/2.0.2/htop-2.0.2.tar.gz
   tar xfz htop-2.0.2.tar.gz
   cd htop-2.0.2
   ./configure
   make
   make install


6. алгоритм предполагаю такой:
- в переменную помещаем имена файлов нашего каталога 
# ls -la /opt/duplicates/ | awk '{print $9}'
- берем из переменной построково имена файлов и выводим циклом в файл-табличку(list), содержащую два столбца: "хэш файла" "имя файла" 
# somefile md5sum >> list
- формируем ещё один список из одних только уникальных хэш-сумм
#  awk '{print $1}' $list | sort | uniq
- сравниваем уникальные хэши по очереди с файлом list и берем имена файлов, у которых совпал хэш с уникальным хэшем
# grep $hash $list | awk '{print $2}'
- удаляем те файлы, хэш которых совпал
# rm -f file
